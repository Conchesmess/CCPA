steps:
# Build the container image
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'us-west1-docker.pkg.dev/${PROJECT_ID}/my-docker-repo/my-image', '.']
  #args: ['build', '-t', 'gcr.io/ccpa-394520/ccpa', '.']
# Push the container image to Container Registry
- name: 'gcr.io/cloud-builders/docker'
  args: ['push', 'us-west1-docker.pkg.dev/${PROJECT_ID}/my-docker-repo/my-image']
# Deploy container image to Cloud Run
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: gcloud
  args: ['run', 'deploy', 'ccpa', '--image=gcr.io/ccpa-394520/ccpa', '--region=us-west1']
images:
- gcr.io/ccpa-394520/ccpa
options:
  logging: CLOUD_LOGGING_ONLY
